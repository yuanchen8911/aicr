# Copyright (c) 2026, NVIDIA CORPORATION & AFFILIATES.  All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: 'Go Lint'
description: 'Run Go linter, YAML linter, and license header checks'

inputs:
  go_version:
    description: 'Go version to install (e.g., 1.25)'
    required: true
  golangci_lint_version:
    description: 'golangci-lint version to use (e.g., v2.6)'
    required: true
  addlicense_version:
    description: 'addlicense version to use (e.g., v1.1.1)'
    required: false
    default: 'v1.1.1'
  lint_timeout:
    description: 'Lint timeout duration'
    required: false
    default: ''

runs:
  using: 'composite'
  steps:
    - name: Setup Go
      uses: actions/setup-go@7a3fe6cf4cb3a834922a1244abfce67bcef6a0c5  # v6.2.0
      with:
        go-version: '${{ inputs.go_version }}'
        cache: true
        cache-dependency-path: |
          go.sum
        check-latest: true

    - name: Lint Go
      if: hashFiles('.golangci.yaml') != ''
      uses: golangci/golangci-lint-action@1e7e51e771db61008b38414a730f564565cf7c20  # v9.2.0
      with:
        version: ${{ inputs.golangci_lint_version }}
        args: --timeout=${{ inputs.lint_timeout || '5m' }}

    - name: Lint YAML
      if: hashFiles('.yamllint.yaml') != ''
      shell: bash
      run: |
        sudo apt-get update && sudo apt-get install -y yamllint
        make lint-yaml

    - name: Install addlicense
      shell: bash
      run: |
        ADDLICENSE_VERSION="${{ inputs.addlicense_version }}"
        ADDLICENSE_VERSION_NUM="${ADDLICENSE_VERSION#v}"
        go install github.com/google/addlicense@${ADDLICENSE_VERSION}
        echo "addlicense installed: $(addlicense -version 2>&1 || echo ${ADDLICENSE_VERSION_NUM})"

    - name: Ensure License Headers
      shell: bash
      run: make license

    # Runs after `make license`, not before: addlicense writes in place, so
    # this ordering also proves the LICENSE_IGNORES entry for the vendored
    # vendoring is doing its job. If a header were stamped into those files, their
    # digests would change and this step would fail.
    - name: Verify vendored Go proxy action
      shell: bash
      run: make check-depproxy-kit
